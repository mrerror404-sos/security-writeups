# OneZero Bank – Clickjacking

The homepage of `onezerobank.com` could be fully loaded inside an iframe, meaning X-Frame-Options and CSP frame-ancestors headers were missing.

Status: N/A (Non-sensitive page)
